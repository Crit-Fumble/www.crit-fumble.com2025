# ============================================
# Local Development with Core API (via Tunnel)
# ============================================
# Copy this file to .env.local when testing against real Core API
#
# Prerequisites:
# 1. Discord test app with localhost redirect URI configured
# 2. doclt CLI installed (or SSH access to Core droplet)
#
# To start (tunnel + dev server together):
#   npm run dev:tunnel
#
# Or manually:
#   doclt compute ssh <core-droplet> -L 9000:localhost:3000
#   npm run dev

# ============================================
# Tunnel Configuration (for dev:tunnel script)
# ============================================
# Droplet name (for doclt) or IP address (for direct SSH)
CORE_DROPLET="core-droplet"

# Tunnel mode: "doclt" (default) or "ssh"
TUNNEL_MODE="doclt"

# Local port for the tunnel (default: 9000)
# TUNNEL_PORT="9000"

# SSH key path (only needed for TUNNEL_MODE="ssh")
# TUNNEL_SSH_KEY="~/.ssh/id_rsa"

# ============================================
# Core API (via SSH Tunnel)
# ============================================
# Local tunnel port forwarding to Core API
CORE_API_URL="http://localhost:9000"

# Use the same secret as staging/production Core
# Get from: doclt or Vercel staging env vars
CORE_API_SECRET=""

# ============================================
# NextAuth
# ============================================
# Generate with: openssl rand -base64 32
AUTH_SECRET=""

# Local development URL
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# Discord OAuth (Test App for localhost)
# ============================================
# Create a SEPARATE Discord app for local development at:
# https://discord.com/developers/applications
#
# Configure OAuth2 redirect URI:
#   http://localhost:3000/api/auth/callback/discord
#
# Required scopes: identify, email, guilds.members.read
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
NEXT_PUBLIC_DISCORD_CLIENT_ID=""

# ============================================
# Permissions
# ============================================
# Your Discord user ID (for admin access during testing)
OWNER_DISCORD_IDS=""
ADMIN_DISCORD_IDS=""

# ============================================
# Early Access
# ============================================
# Discord server IDs that have early access
# Include your test server ID here
ALLOWED_GUILD_IDS=""

# ============================================
# IMPORTANT: Disable Mock Auth
# ============================================
# Must be false (or unset) to use real Core API
USE_MOCK_AUTH="false"

# ============================================
# Optional
# ============================================
# Show staging banner locally
APP_ENV="development"

# FumbleBot secret (if testing bot integration)
# BOT_API_SECRET=""
